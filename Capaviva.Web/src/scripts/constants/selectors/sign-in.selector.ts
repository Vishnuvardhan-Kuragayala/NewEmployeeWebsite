const PREFIX = "sign-in__";

const SignInSelectors = {
	EMAIL: PREFIX + "txt-email",
	PASSWORD: PREFIX + "txt-password",
	NEW_PASSWORD: PREFIX + "txt-newpassword",
	CONFIRM_PASSWORD: PREFIX + "txt-confirmpassword"
};

export default SignInSelectors;
