export enum ButtonColor {
	DEFAULT = "default",
	INHERIT = "inherit",
	PRIMARY = "primary",
	SECONDARY = "secondary"
}

export enum TextboxColor {
	PRIMARY = "primary",
	SECONDARY = "secondary"
}

export enum LinkColor {
	INITIAL = "initial",
	INHERIT = "inherit",
	PRIMARY = "primary",
	SECONDARY = "secondary",
	TEXT_PRIMARY = "textPrimary",
	TEXT_SECONDARY = "textSecondary",
	ERROR = "error"
}
